name: Daily xPts Update

on:
  schedule:
    # 04:00 UTC daily (06:00 RO CET / 07:00 CEST)
    - cron: '0 4 * * *'
  workflow_dispatch:  # manual trigger

permissions:
  contents: write  # commit xg/db/xpts.db back to repo

jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: xg/requirements.txt

      - name: Install xg pipeline dependencies
        working-directory: xg
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run pipeline (UCL)
        working-directory: xg
        run: |
          python -c "from src.pipeline.update import run_full_update; run_full_update(competition_id=1, scoring_system='ucl', skip_fbref=True)"

      - name: Commit updated DB
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add xg/db/xpts.db
          if git diff --cached --quiet; then
            echo "No DB changes to commit"
            exit 0
          fi
          git commit -m "auto: daily xPts pipeline update ($(date -u +%Y-%m-%dT%H:%MZ))"
          git pull --rebase origin main
          git push
